<section class="ShowUser">
	<h3 class="ShowUser-title">User</h3>
	<p><strong>Email:</strong> <%= @user.email if @user.email %></p>

	<%= link_to 'Edit', edit_admin_user_path(@user), class: "ShowUser-edit" %> |
	<%= link_to 'Back', admin_users_path, class: 'ShowUser-back' %>
</section>	
