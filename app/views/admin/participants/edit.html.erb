<%= javascript_include_tag 'participants/validate-form' %>

<h1 class="title">Editing Participant</h1>

<%= render 'form', participant: @participant, url: admin_participant_path %>

<%= link_to 'Show', admin_participant_path(@participant), class: 'EditingParticipant-show' %> |
<%= link_to 'Back', admin_participants_path, class: 'EditingParticipant-back' %>

<script type='text/javascript'>
	validateForm($(".edit_participant"));
</script>
